**Step 0.** Install dependencies `pcl` & `grid-map`

**Step 1.** Put your `.pcd` file to `<path-to-pcd-processor>/data`

**Step 2.** `catkin build` and `ros launch pcd_processor pcd_processor.launch`
